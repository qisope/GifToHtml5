What should the input to the transform process be?
 - just the url to the original
   - this keeps things simple
   - no database access requirement

Accept input file url
open target url as a readstream
 - fs.createReadStream('url...');
 - new FFmpeg({source: stream});


